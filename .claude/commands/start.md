Read `CLAUDE.md` and `docs/PHASES.md`.

Report back, briefly:
- Current phase and Active Task
- Last Completed task
- Any open blockers listed in CLAUDE.md that affect the next task

Do not begin building. If the next task is a page build, check `docs/PAGES.md`
for that page's spec (layout, copy status, interaction) before reporting —
flag if the page is blocked on missing copy rather than proceeding.
