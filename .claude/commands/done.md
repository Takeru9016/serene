Argument: $ARGUMENTS (the task just completed)

1. Commit with message format `page: short description`
2. Update `docs/PHASES.md` — check off the completed item, move Active Task
   to the next unblocked item in the list
3. Update CLAUDE.md's "Current Phase" pointer if the phase changed
4. Report the next task, and flag clearly if it's blocked (gift content,
   poetry, reasons) rather than starting it
