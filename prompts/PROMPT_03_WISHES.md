# Phase 3 — Wishes (`/wishes`)

Paste into Claude Code once Phase 2 is checked and committed.

---

Build `src/app/wishes/page.tsx` per `docs/PAGES.md` — Wishes.

1. Eyebrow + H2, then the 5 wish cards from `content.ts` `wishes.items` —
   map over the array, don't hardcode 5 separate blocks
2. GSAP stagger reveal on scroll into view — this page is content-led, no
   glow/texture beyond the site-wide base grain
3. `Continue →` link to `/gift`, label can vary from the generic string per
   `docs/PAGES.md`
4. Progress dots: 1 of 5, filled

State your plan before writing code. Run `/check` once built.
