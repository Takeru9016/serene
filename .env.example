# See docs/DOMAIN_AND_ACCESS.md for how these are used. Never commit real
# values — set them in Vercel's dashboard only.

# What she types on the /gate form.
GATE_PASSCODE=

# Long random secret stored in the access cookie. Generate with:
#   openssl rand -hex 32
GATE_TOKEN=
